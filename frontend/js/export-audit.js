(() => { let page = 1; const size = 25; const esc = value => String(value ?? '').replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]); const load = async () => { const body = document.getElementById('exportAuditRows'); try { const response = await window.ACEAuth.request(`/v1/export-audit?page=${page}&pageSize=${size}`); const items = response.items || []; body.innerHTML = items.length ? items.map(item => `<tr><td>${new Date(item.created_at).toLocaleString()}</td><td>${esc(item.profiles?.full_name || item.profiles?.email || 'Administrator')}</td><td>${esc(String(item.action).replace(/_/g, ' '))}</td><td>${esc(item.description || '—')}</td></tr>`).join('') : '<tr class="table-empty-row"><td colspan="4"><div class="export-audit-empty"><img src="assets/icons/download.svg" alt="" aria-hidden="true"><div><strong>No exports recorded yet</strong><p>When an administrator exports Time Entries or saves a report as PDF or Excel, it will be recorded here with the person, format, and selected date range.</p><a class="btn btn-sm btn-outline" href="admin-time-entries.html">Export time entries</a></div></div></td></tr>'; const pager = document.getElementById('exportAuditPagination'); const pages = Math.max(1, Math.ceil((response.total || 0) / size)); pager.innerHTML = response.total > size ? `<button data-page="${page - 1}" ${page === 1 ? 'disabled' : ''}>Previous</button><span>Page ${page} of ${pages}</span><button data-page="${page + 1}" ${page === pages ? 'disabled' : ''}>Next</button>` : ''; pager.querySelectorAll('[data-page]').forEach(button => button.addEventListener('click', () => { page = Number(button.dataset.page); load(); })); } catch (error) { body.innerHTML = `<tr><td colspan="4">${esc(error.message || 'Could not load export audit.')}</td></tr>`; } }; window.mountExportAudit = load; document.addEventListener('DOMContentLoaded', load); })();
